{
  "name": "codeek-ai-website",
  "type": "module",
  "version": "1.0.0",
  "scripts": {
    "dev": "astro dev",
    "build:icons": "node scripts/generate-icons.mjs",
    "build": "node scripts/generate-icons.mjs && astro build && node scripts/sync-root-build.mjs",
    "preview": "astro preview"
  },
  "dependencies": {
    "@fontsource-variable/inter": "^5.1.0"
  },
  "devDependencies": {
    "@astrojs/sitemap": "^3.0.0",
    "@tailwindcss/vite": "^4.0.0",
    "astro": "^5.0.0",
    "sharp": "^0.33.0",
    "tailwindcss": "^4.0.0"
  }
}
